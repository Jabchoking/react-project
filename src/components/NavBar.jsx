import React, { memo } from 'react';
import { NavBardiv } from '../assets/css/MusicSub';



const NavBar = memo(() => {
    return (
        <NavBardiv>
            test
        </NavBardiv>
    );
});

export default NavBar;