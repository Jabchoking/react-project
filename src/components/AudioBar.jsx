import React, { memo } from 'react';
import { AudioBardiv } from '../assets/css/MusicSub';


const AudioBar = memo(() => {
    return (
        <AudioBardiv>
            오디오
        </AudioBardiv>
    );
});

export default AudioBar;